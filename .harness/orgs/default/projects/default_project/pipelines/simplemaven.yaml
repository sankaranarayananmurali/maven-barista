pipeline:
  name: simple-maven
  identifier: simplemaven
  projectIdentifier: default_project
  orgIdentifier: default
  tags: {}
  properties:
    ci:
      codebase:
        connectorRef: mavengithubconnector
        build: <+input>
  stages:
    - stage:
        name: mvn-build
        identifier: mvnbuild
        description: ""
        type: CI
        spec:
          cloneCodebase: true
          caching:
            enabled: true
            override: true
          buildIntelligence:
            enabled: true
          platform:
            os: Linux
            arch: Amd64
          runtime:
            type: Docker
            spec: {}
          execution:
            steps:
              - step:
                  type: Run
                  name: Run_1
                  identifier: Run_1
                  spec:
                    connectorRef: dockerhub
                    image: sankaranarayananmurali/petclinic-sam:3.4.0
                    shell: Sh
                    command: mvn clean test
        delegateSelectors:
          - docker-delegate
